replicaCount: 1

image:
  repository: mi-local-image
  tag: wso2mi_410_sit0001
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 8290
  nodePort: 30290

management:
  port: 9164

truststore:
  mountPath: /home/wso2carbon/security
  fileName: client-truststore.jks

resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1"
