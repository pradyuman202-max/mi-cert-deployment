# ── Dev Environment Values ───────────────────────────────────────────────────
# This file is auto-updated by the Jenkins pipeline (image.tag field).
# Do NOT manually hardcode the tag here — it is managed by CI/CD.
# ─────────────────────────────────────────────────────────────────────────────

cat > /home/svc_account_wso2/SIT_MI_Docker_Project/values-dev.yaml << 'EOF'
replicaCount: 1

image:
  repository: mi-sit
  tag: sit0001
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 8290
  nodePort: 30290

management:
  port: 9164

truststore:
  mountPath: /home/wso2carbon/security
  fileName: client-truststore.jks

resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1"

env: dev
debug: true
EOF
